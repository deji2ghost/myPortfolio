import styled from 'styled-components';




export const LineContainer = styled.div`
    background-color: ${({theme})=>theme.ash};
    height: .9px;
    max-width: ${({theme})=>theme.maxWidth};
    margin: 0 auto;
`